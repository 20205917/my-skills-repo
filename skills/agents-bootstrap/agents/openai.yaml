version: 1
skill:
  display_name: AGENTS 初始化
  short_description: 提示词优先生成项目级 AGENTS.md（事实采集 + Prompt Contract + 草稿校验 + 差异预览），全局级按模板初始化。
  default_prompt: 使用 $agents-bootstrap 为当前仓库生成项目级 AGENTS.md：先采集项目事实并生成 Prompt Contract，再基于模型草稿执行 dry-run 校验与差异预览，确认后再写入；全局级仅在我明确要求时按模板生成。
