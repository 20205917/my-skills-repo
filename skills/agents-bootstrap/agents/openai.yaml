version: 1
skill:
  display_name: AGENTS 初始化
  short_description: 模板优先初始化 AGENTS.md，默认项目级并自动更新 .gitignore，按需生成全局级。
  default_prompt: 使用 $agents-bootstrap 为当前仓库初始化项目级 AGENTS.md（模板优先），先 dry-run 预览，再写入并确认 .gitignore 与加载优先级；仅在我明确说明时生成全局级。
